import React from 'react';
import Lobby from './lobby';

export default ()=>{
    return(
        <div>
            {/*<Player />*/}
            <Lobby history={this.props.history}/>
        </div>
    )
}